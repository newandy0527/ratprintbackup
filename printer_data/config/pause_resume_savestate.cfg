[gcode_macro PAUSE_SAVESTATE]
#rename_existing: BASE_PAUSE_SAVESTATE
variable_zhop: 0
variable_etemp: 0
gcode:
  {% set z = params.Z|default(10)|int %}
  {% if printer['pause_resume'].is_paused|int == 0 %}
    # Save Z-hop and hotend temp for resume
    SET_GCODE_VARIABLE MACRO=RESUME VARIABLE=zhop VALUE={z}
    SET_GCODE_VARIABLE MACRO=RESUME VARIABLE=etemp VALUE={printer['extruder'].target}

    # Optional: disable filament sensor during pause
    # SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=0

    # Save current position
    SAVE_GCODE_STATE NAME=PAUSE

    BASE_PAUSE

    # Z-hop if within limits
    {% if (printer.gcode_move.position.z + z) < printer.toolhead.axis_maximum.z %}
      G91
      G1 Z{z} F900
    {% else %}
      { action_respond_info("Pause z-hop exceeds max Z height") }
      SET_GCODE_VARIABLE MACRO=RESUME VARIABLE=zhop VALUE=0
    {% endif %}

    G90
    G1 X{printer.toolhead.axis_maximum.x/2} Y{printer.toolhead.axis_minimum.y+5} F6000

    # Save parked position
    SAVE_GCODE_STATE NAME=PAUSEPARK

    # Turn off heaters
    M104 S0
    M140 S0

    # Extend idle timeout (e.g., 12 hours)
    SET_IDLE_TIMEOUT TIMEOUT=43200
  {% endif %}

[gcode_macro RESUME_SAVESTATE]
#rename_existing: BASE_RESUME_SAVESTATE
variable_zhop: 0
variable_etemp: 0
gcode:
  {% set e = params.E|default(2.5)|float %}
  {% if printer['pause_resume'].is_paused|int == 1 %}
    # Re-enable filament sensor if used
    # SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=1

    # Restore idle timeout
    SET_IDLE_TIMEOUT TIMEOUT={printer.configfile.settings.idle_timeout.timeout}

    # Reheat hotend if needed
    {% if etemp > 0 %}
      M109 S{etemp|int}
    {% endif %}

    # Return to parked position
    RESTORE_GCODE_STATE NAME=PAUSEPARK MOVE=1 MOVE_SPEED=100

    # Prime nozzle and lower Z
    G91
    M83
    {% if printer[printer.toolhead.extruder].temperature >= printer.configfile.settings.extruder.min_extrude_temp %}
      G1 Z{zhop * -1} E{e} F900
    {% else %}
      G1 Z{zhop * -1} F900
    {% endif %}

    # Restore original print position
    RESTORE_GCODE_STATE NAME=PAUSE MOVE=1 MOVE_SPEED=60
    BASE_RESUME
  {% endif %}

