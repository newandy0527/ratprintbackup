[gcode_macro _CUTTER_SERVO POS=out]
description: Sweep the tool_cut servo across its full range
gcode:
    {% for angle in range(0, 181, 30) %}
    SET_SERVO SERVO=tool_cut ANGLE={angle}
    G4 P1000
    {% endfor %}
    SET_SERVO SERVO=tool_cut ANGLE=90